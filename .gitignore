*.DS_Store
vscode/tasks.json
**launch.json
*.swp

# Active ghostty theme include — a symlink to theme-{dark,light}.conf, flipped
# by toggle_theme.sh. Per-machine state, not tracked.
ghostty/theme-active.conf
